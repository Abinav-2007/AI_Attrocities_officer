# Flutter conversion

This folder contains a Flutter version of the Sahyog AI officer interface.

## Run

```bash
cd flutter_app
flutter pub get
flutter run
```

## Scope

- Route-equivalent pages for all React routes
- Shared mock data ported from `src/data/mockData.ts`
- Dashboard shell, KPI cards, lists, and chart widgets implemented in Flutter
